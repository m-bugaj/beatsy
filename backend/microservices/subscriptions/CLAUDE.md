# subscriptions

Szkielet, bez configu portu nawet (brak `application-dev.properties`) — tylko `SubscriptionsApplication` + test `contextLoads()`, najmniej ruszony ze wszystkich serwisów. Zero logiki, zero integracji — nie zakładać niczego o kształcie tej domeny bez pytania, po prostu jeszcze nie istnieje.
