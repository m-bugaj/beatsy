# fulfillment-service

Szkielet, port 8088 (dev) — tylko `FulfillmentServiceApplication` + test `contextLoads()`, brak jakiejkolwiek logiki biznesowej. Do zbudowania: prawdopodobnie dostarczanie zakupionej treści po udanej płatności (konsument `Order`/`payment-service`), ale to na razie tylko wynika z nazwy i miejsca w mapie serwisów w root `CLAUDE.md`, nie z kodu.
