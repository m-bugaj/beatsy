# license-service

Szkielet, port 8089 (dev) — tylko `LicenseServiceApplication` + test `contextLoads()`, brak logiki biznesowej. Uwaga: `marketplace-service` ma już własne `LicenseTemplate`/`LicenseLimitConfig` i `LicenseClient` (patrz `marketplace/CLAUDE.md`) — jeśli/gdy ten serwis zacznie żyć, sprawdź najpierw, czy nie duplikuje odpowiedzialności już zaimplementowanej w `marketplace`.
